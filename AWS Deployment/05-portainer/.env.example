# Portainer Configuration
# Note: Portainer does not support environment variables for initial user creation.
# You must create the admin user through the web interface on first access.

# These are reference values for documentation only
PORTAINER_USERNAME=arpansahu
PORTAINER_PASSWORD=your_secure_password_here
PORTAINER_PORT=9443
