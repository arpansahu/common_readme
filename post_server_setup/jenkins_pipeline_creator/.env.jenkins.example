# Jenkins API Credentials for Pipeline Creation
# Copy this to .env.jenkins and fill in your credentials

# Jenkins URL (without trailing slash)
JENKINS_URL=https://jenkins.arpansahu.space

# Jenkins username for API access (typically 'admin')
JENKINS_USER=admin

# Jenkins API token (generate from: Jenkins > User > Configure > API Token)
# Also known as "Legacy API Token" in Jenkins
JENKINS_API_TOKEN=your_api_token_here

# Jenkins CLI jar location (will be downloaded automatically if not found)
JENKINS_CLI=/tmp/jenkins-cli.jar
